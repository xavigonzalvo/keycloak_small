# =============================================================================
# Single source of truth for the public-facing hostname.
# Change this to your FQDN for production (e.g. app.example.com).
# =============================================================================
APP_FQDN=localhost
